# Recoverable legacy README archival

The complete previous root README was copied before its navigation rewrite.
Both source and copy were observed to have SHA-256
`0525d1e951a7b510fa6e67b1e3835d42121a5d5dd43041a15bc0cb0dcc4b13e6`
(20396 bytes,314 lines; byte hash check tool chunk ed01f1 exit0).
The unchanged copy is `README.pre_manuscript_acceptance.md` in this directory.
Its relative links still describe the original repository-root location.
The filename is a historical label: this snapshot includes the then-current
manuscript status header and the full legacy README below it. It predates the
new navigation rewrite, not the successful manuscript-artifact gate itself.

The root README is now current navigation and explicitly limited v5 status,
not a carrier for historical accuracy, equivalence, deployment or energy claims.
This is a reversible documentation change: the full prior text is recoverable
from the byte-identical copy. No model, dataset, result, source-bound review,
old manuscript, or existing archive was moved or deleted.

Before the rewrite, a read-only membership check (28c690 exit0) confirmed that
the mutable root README was not one of the accepted manuscript's4111 held
ordinary inputs. Those committed artifacts remain in their canonical paths.
This archival record is documentation, not a new research/release certificate.
