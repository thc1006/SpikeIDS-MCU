# Portable host protocol / CLI independent review

No remaining concrete blocker in the bounded offline scope. Final run `211126`/exit **0** passed **43 tests plus 26 subtests** in 0.56 s. It combines 8 independent whole-CLI controls, 23 transport controls and the author's 8 protocol / 4 C-wire cross-controls in one process. Thirteen source/test/C ABI files matched before/after (`009d4b`/0); exact hashes, scope and XML are in `PEER_FINAL_EXECUTION.json`.

The new independent CLI fixture uses real Store/fsync, CLI main, Session, request/response codec and full-row validation, with synthetic 1,024 distinct 64-bit row IDs and 41-word inputs, five reference words, and an opaque context-managed serial exchange. Temporary source/output files are real; serial access is not. It checks all last-row words and limited RESULT flags, close/parity/source failures before acceptance, final-row retention failure poisoning, no existing-output adoption and offline no-open behavior. Author cross-tests compile real shared wire C with a fake inference function; the combined suite also reads the fixed original validation NPZ offline. No model computation, target code or USB operation occurs, so the suite is not inaccurately described as entirely synthetic.

Two finite source-lifetime counterexamples were reproduced against run_esp `64395a…`: source modification during load_rows was freshly adopted by late source capture, and modification by the RESULT writer after the last source check still returned success. `4a32c7`/1 and its two-failure XML are preserved. Root's c871e3… fix captures sources at entry, checks after input preparation and before transport, after close and after RESULT publication. Both controls now pass. A late publication failure may preserve a provisional RESULT alongside FAILED and nonzero exit; RESULT presence is not accepted independently of the external exit. This does not claim immutable archives or atomic protection from future/privileged changes.

Static wire review agrees with the declared 12/160-byte HELLO and 232/88-byte inference framing, little-endian 64-bit row IDs, selected graph/vector identity, CRC and exact 41→5 shape. HELLO board IDs 1/2 denote the declared RA/ESP protocol roles, not an independently measured physical SKU or authenticated image. No nonce/freshness or firmware-readback proof is claimed. The session rejects erroneous responses without inference retry. The CLI preconstructs all 1,024 requests before opening, requires the original complete ordered run, preserves every row before continuing, and retains the original NumPy FP32 `isclose(reference, actual)` threshold direction and argmax check. Retention failures poison the session. Results keep firmware-readback, latency, energy and research acceptance false.

Host time/noise bounds are engineering safeguards, not electrical limits. The serial wrapper's documented OS DTR/RTS and pre-open-buffer limitations remain. Actual physical board/probe selection, firmware programming/readback, stable power, real timing, arithmetic behavior and hardware acceptance are outside this review. Only new tests and review files were edited by this reviewer; root protocol/validation/CLI fixes were made by root, with original failed evidence retained.
