# SRAM adapter independent offline preflight

No remaining concrete blocker to **one separately root-authorized offline ARM build** of build source `37194ebe0a164873310855e11ec911f8acab468e1728e1c382addf31808d2872`. This does not authorize loading or executing the image. Generated ELF/map/disassembly still require saved-only review afterward.

Independent final controls: **25 passed**, actual tool chunk `ce0dd8`, exit **0**, 0.05 s; scope `spikeids-n6-sram-independent-final03.scope`, invocation `3a2baa4b6c7d4acb8388fb4710b4a3da`, MemoryMax 4 GiB / MemorySwapMax 0. Eight source/document hashes and the test hash matched before this run and afterward (`3c2396`, exit 0). Test SHA `cee69b7f11903513d351911f8cc7df202dacd23dd5c887f287067c088a1ecad0`; final XML `cf79ccd29e94d565cbe485fb575da9c02aaf0645014e89f340a5e5db02c87eff`.

Concrete findings preserved and fixed:

- `d7fd3d` / 0 proved the initial ELF magic compared literal backslashes rather than ELF bytes. The valid independent four-segment ELF now passes.
- `7eefab` / 1 retained 22 pass / 2 fail: late added retained-candidate directory was accepted; rebound output parent caused a rejected build to create a foreign directory. Final original namespace checks and original-parent/dir-fd creation reject both controls.
- `efe021` / 1 proved fresh source capture overwrote an original candidate pin and reached the fake compiler. The new `hold()` compares existing commitments instead, including selected tool/library paths. Final control rejects before any child starts.

Reviewed contracts: same QDQ SHA `22dc7979340c34af613840a8cef70bc07f469ae2143925660cf3312f36475e2d`, same 145457-byte RAW SHA `cb5b641344e146a9a1b3d439953c9c3b078c706f5fa55eee40b5339ed2cb65ec`; saved review `e024e446...`, external exit `30cb08b2...` and original snapshot `ddf5338a...` are literal whole-file commitments. Original generation wrapper exit 1, child exit 0, FAILED/no RESULT are preserved, not reclassified. Linker and ELF checks exclude the full 256 KiB weights and 16 KiB activation pools, not merely 2048 used or 2112 prefetch bytes. New S6/SM01 mailbox identity distinguishes this mutually exclusive replacement from v5; old external/activation addresses are not reused.

New main source `7af7972cc89b2137a2a6d7cee80ccdd69209d77c33f4ce0225863869bee6bf94` checks all 41 input words and request fields before and after inference; all five finite FP32 result words are exposed. DONE precedes DMB and the final response-sequence commit; the host contract requires a stable matching DONE/sequence snapshot, not sequence alone. This is a cooperative one-request protocol, not a hostile concurrent-writer proof. Startup places CPACR enable before optimized C. Privileged Secure Thread entry and correct loader/cache/security/watchdog state remain prerequisites; the C fault handler remains best-effort and depends on a usable stack. WAIT_PLATFORM acknowledges prerequisites but does not establish them.

Scope limits: real tiny file I/O and synthetic ELF parser calls; synthetic historical receipt paths/hashes in the candidate fixture; opaque `candidate_pins()` only in run-entry controls; fake Popen for child-failure retention. Mailbox/startup checks are source-contract checks, not compiled-C execution or CPU memory-model simulation. No full successful fake compiler pipeline, true compiler, original tensor loading, inference, USB/SWD, target execution, or hardware acceptance occurred. Finite stat/hash/namespace bookends do not prevent arbitrary later privileged changes. All eight hardware/deployment/parity acceptance flags remain false.
