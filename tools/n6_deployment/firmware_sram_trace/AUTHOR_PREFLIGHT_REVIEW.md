# Author preflight: additive S6 callback-trace adapter

Observed `ebb197 / exit 0`: all 21 fixed synthetic/source controls passed, child exit 0 (0.014 s), 19 new/frozen source/test hashes identical before and after. Full raw streams and both hash maps are in `AUTHOR_PREFLIGHT_EXECUTION.json`. Earlier unbookended development run `9d01de / exit 0` also passed; the final source-bound run is the one cited here.

This is author verification, not independent signoff. No ARM or host C compiler was invoked by these tests, no genuine inference was run, and no device was opened. The production build command has not been invoked at this preflight point.

Narrow delta from frozen S6: unchanged startup/syscalls; new mailbox magic/tag plus seven tail words without moving the first 482 bytes; one dedicated 6,556-byte NOLOAD region and fifth PT_LOAD; exact frozen callback registration after init; begin before sync; finish with the actual return before any error park; original API status and raw five-output preservation even for returned runtime failure. No generated model, weights, runtime, finite-input/output rule, ordered request rule or numerical tolerance was changed. The builder retains the original fixed generation-history/tool/runtime helpers byte-for-byte at AST level, binds original S6 and all three callback sources by literal hashes, adds their actual compilation/linkage, and verifies the complete reserved pools plus the exact new segment.

Self-adversarial controls reject missing/initialized/writable-executable/misplaced/wrong-size trace segments, mailbox expansion, stack/vector changes and weights/activation overlap. They also check no CLI overrides or stale output, original frozen source commitments, same-bytes replacement rejection, unchanged mailbox prefix, actual-return finish ordering, full output/status retention before failure and unchanged request/finite gates. Source tests are lexical/AST controls, not simulated C runtime execution. The retained frozen callback module's own 38 host-C controls/ARM objects are separate evidence, not proof that this full variant links or runs.

Remaining bounded requirements: independent review, one authorized real offline compile/link, then saved ELF/layout/pin inspection; a new trace-aware host that retains each complete log before committing the next request; qualified platform/power/loader and subsequent on-target full-output checks. Current old S6 host must reject the new magic. ERROR has no new response_sequence success commit; only matching actual trace.run_id may be attributed to a request. Early pre-begin errors and FAULTs cannot be relabeled completed traces. CPU halt is not NPU quiescence or a power/safety proof.

There is no target-side trace-retained ACK: the host's immutable one-request protocol and retain-before-next obligation are explicit. Callbacks cannot stop runtime hardware and can perturb execution. DWT reads are raw modular CPU observations, not independently proven NPU cycles, and no performance/energy/board acceptance is made. The inherited finite filesystem bookends do not assert hostile-filesystem or full-host attestation.

Candidate held source SHA-256: main `6fc7bbee84cd33698d6842f9478269d73a56690edcfb6225c3ccb29f288f305c`; builder `8041f720bfac876f010382f8324439ccd8ab251687e3540c01bfc0435bc5994d`; linker `eba4e07b47a5ff680d73693a996f704d663fd4e8cea24283134fc47d72e55f5f`; ABI `306f156c6efc25c15961b8831130d2d7d129c930457ec696f28ee400c2c45a9d`; frozen callback core `c1125ead0369ef7666e06568f3f0c2ac9efb955eaa57369402aa7c038db6bcbf`.
